#include <stdio.h>

/**
*main: set n to 98
*@n: input
*return:sucess 1:wq
*/

int main(void)
{
	int n;
	int *p;

	*p = &n
	n = 402;
	printf("n=%d\n", n);
	reset_to_98(&n);
	printf("n=%d\n", n);
	return (0);
}
